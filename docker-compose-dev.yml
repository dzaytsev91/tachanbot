services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/memes.db
      BACKUP_DIR: /data/backups
      TZ: Europe/Moscow
    volumes:
      - tachanbot_dev_data:/data

volumes:
  tachanbot_dev_data:
