#!/usr/bin/env bash
set -euo pipefail

# This repository has no services or authentication that need reconnecting.
# Python dependencies and system tools are restored from the orb snapshot.
printf 'tachanbot orb environment is ready.\n'
